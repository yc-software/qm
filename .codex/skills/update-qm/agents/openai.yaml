interface:
  display_name: "Update QM"
  short_description: "Merge upstream qm into this private fork"
  default_prompt: "Use $update-qm to sync this private fork with upstream qm and open the sync PR."
