# Browser Task screenshot retention

I want Browser Task screenshots to help project members see and summarize whether work was completed.

Each Project should be able to choose a screenshot retention period from 1 to 90 days, with 30 days as the default. Completed Browser Tasks should expose screenshots through screenshot artifacts separately from result text, so the screenshot tool can retrieve them without putting screenshot bytes into the text result.
